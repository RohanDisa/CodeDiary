# GitHub – choose one:

# Option A: Personal Access Token (fully CLI, no browser)
# Create at https://github.com/settings/tokens – scope: read:user, repo
# GITHUB_TOKEN=ghp_xxx

# Option B: OAuth device flow (opens GitHub login page, enter code – no localhost)
# Create OAuth App at https://github.com/settings/developers
# Enable "Device flow" in the app's settings
GITHUB_CLIENT_ID=your_client_id

# Gemini (for summarization) - get key from https://aistudio.google.com/apikey
# GEMINI_API_KEY=xxx
# or for Vertex AI: GOOGLE_GENAI_USE_VERTEXAI=true, GOOGLE_CLOUD_PROJECT=xxx, GOOGLE_CLOUD_LOCATION=us-central1

# Notion - create integration, share a page with it
# NOTION_API_KEY=secret_xxx
# NOTION_PAGE_ID=xxx   (all PR summaries are appended to this page)

